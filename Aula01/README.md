# Hello World

```rust
fn main() {
    println!("Hello, world!");
}
```

### Cargo.lock / Cargo.toml

São os arquivos de versionamento do rust

### Target

    cargo build

É a pasta dos dados compilados ( nele fica o binário )